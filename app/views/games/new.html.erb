
<div class="d-flex justify-content-center mb-8">
  <h1>New Game</h1>
</div>


<div class="d-flex justify-content-around mt-6">
  <% @letters.each do |letter| %>
    <div class="p-3 mb-2 bg-primary text-white border border-dark rounded shadow"><%= letter %></div>
  <% end %>
</div>

<br>
<div class="d-flex justify-content-center mb-3">
<p>What's the longest word you can think of using each of the above letters once?</p>
</div>

<form class="mt-3" action="/score" method="post" data-turbo="false">
  <div class="input-group mb-3">
    <input type="text" name="word" value="<%= params[:word] %>" class="form-control" placeholder="your word" aria-label="guess" aria-describedby="basic-addon2">
    <div class="input-group-append">
      <button class="btn btn-outline-secondary" value="play" type="sumbit">Play</button>
    </div>
  </div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
</form>
